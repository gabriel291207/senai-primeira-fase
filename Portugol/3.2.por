programa {
  funcao inicio() {
    //3.2
    real base, altura, area
    escreva("b(m)= ")
    leia (base)
    escreva("h(m) = ")
    leia (altura) 
    area = (base*altura)/2
    escreva("A área é de: "+area+" metros²")
  }
}
